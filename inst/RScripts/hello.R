#' Sum of vector elements
#'
#' @description
#' This is an example function named 'hello' which prints 'Hello, 
#' world!' You can learn more about package authoring with RStudio 
#' at: http://r-pkgs.had.co.nz/
#' 
#' @details
#' This is a generic function: methods can be defined for it directly
#' or via the [Summary()] group generic. For this to work properly,
#' the arguments `...` should be unnamed, and dispatch is on the
#' first argument.
#' 

hello <- function() {
  print("Hello, world! This is a github package for investigating google scholar data.")
}
